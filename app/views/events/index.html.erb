<% @events.each do |event| %>
<span><% event = event.as_json   %></span>
<span><%= event["description"]   %></span>
<span><%= event["location"]   %></span>
<% end -%>
<span><%= link_to "Edit", edit_event_path("") %></span><br\>
<span><%= link_to "New", new_event_path %></span>